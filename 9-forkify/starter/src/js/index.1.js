import strng from './models/Search'; 
import * as searchView from './views/searchView';
console.log(`Using imported functions ${searchView.add(searchView.ID, 2)} and multiply ${searchView.multiply(2, 6)} and strng ${strng}`);

// import { add, multiply, ID } from './views/searchView';
// console.log(`Using imported functions ${add(ID, 2)} and multiply ${multiply(2, 6)} and strng ${strng}`);